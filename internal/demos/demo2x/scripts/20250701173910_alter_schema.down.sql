-- reverse -- ALTER TABLE "infos" ALTER COLUMN "cate" TYPE text USING "cate"::text;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO

-- reverse -- ALTER TABLE "users" ALTER COLUMN "score" TYPE text USING "score"::text;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO
