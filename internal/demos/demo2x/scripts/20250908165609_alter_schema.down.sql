-- reverse -- ALTER TABLE "infos" ALTER COLUMN "cate" TYPE bigint USING "cate"::bigint;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO

-- reverse -- ALTER TABLE "users" ALTER COLUMN "rank" TYPE text USING "rank"::text;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO
