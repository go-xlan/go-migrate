-- reverse -- ALTER TABLE `infos` MODIFY COLUMN `cate` tinyint;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO

-- reverse -- ALTER TABLE `users` MODIFY COLUMN `rank` longtext;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO
