-- reverse -- ALTER TABLE `infos` MODIFY COLUMN `cate` longtext;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO

-- reverse -- ALTER TABLE `users` MODIFY COLUMN `score` longtext;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO
