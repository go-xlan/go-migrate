-- reverse -- ALTER TABLE `infos` MODIFY COLUMN `cate` bigint;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO

-- reverse -- ALTER TABLE `users` MODIFY COLUMN `score` double;
SELECT TODO / PANIC / RAISE / THROW; -- ALTER TABLE; -- TODO
